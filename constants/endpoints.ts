export const SUPABASE_TABLES = {
  PRODUCTS: "product",
  CATEGORIES: "category",
} as const;

export const MOCK_DATA = {
  BEST_SELLERS: "mock_best_seller",
  RECOMMENDS: "mock_recommend",
} as const;
